root = true

[*]
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true
charset = utf-8

# Shell scripts use tabs (matches the existing codebase).
[*.{sh,tmux}]
indent_style = tab

[*.md]
trim_trailing_whitespace = false
